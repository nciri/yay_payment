const pool = require('../db');

const Refund = {
    initiateRefund: async (transactionId) => {
        // Logique pour traiter le remboursement
    },
    // Autres méthodes selon vos besoins
};

module.exports = Refund;
